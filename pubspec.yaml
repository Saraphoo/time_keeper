name: time_keeper
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  cupertino_icons: ^1.0.2
  pumli: ^0.1.5
  code_uml: ^0.0.1
  sqflite: ^2.2.0
  path_provider: ^2.0.0

dev_dependencies:
  mockito: ^5.0.0
  flutter_test:
    sdk: flutter
  test: any
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
